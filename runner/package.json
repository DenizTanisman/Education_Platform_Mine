{
  "name": "iau-runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sandbox orchestration service for IAU AI platform (Faz 2 + Faz 4).",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types --no-warnings=ExperimentalWarning tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
